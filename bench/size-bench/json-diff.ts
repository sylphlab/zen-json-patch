// @ts-ignore - Might have type issues
import { diff } from 'json-diff'; // Note: json-diff might have different export, adjust if needed

// Use the import to prevent tree-shaking
console.log(diff);
